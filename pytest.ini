[pytest]
addopts = -ra
python_files = test_*.py
# Let pytest discover across repo; include top-level tests package explicitly
testpaths =
    tests
    server/tests
    cv_engine/tests
markers =
    smoke: quick functional smoke tests
